package main

import (
	"encoding/json"
	"errors"
	"fmt"
	"testing"
)

func TestName(t *testing.T) {

	fmt.Println(411341)

	str, err := json.Marshal(nil)
	if err != nil {
		errors.New("fdsfsd")
	}

}
